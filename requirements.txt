trimesh>=4.0.0 # Dùng cho tính toán mesh 3D
numpy>=1.24.0 # Dùng cho tính toán số học và xử lý mảng
scipy>=1.11.0 # Dùng cho tính toán mesh
Pillow>=10.0.0 # Dùng cho xử lý ảnh thumbnail và watermark
reportlab>=4.0.0 # Dùng để export ra file PDF
PyQt5>=5.15.0 # Dùng để tạo giao diện đồ họa (GUI)
fast-simplification>=0.1.6 # Dùng để giảm số lượng đa giác (faces) của mesh
shapely>=2.0.0 # Dùng để kiểm tra overlap giữa các panel khi unfold
pytest>=7.0.0 # Test suite cho pipeline papercraft
diffusers>=0.27.0 # v1.4: Stable Diffusion inpainting để sinh ảnh mặt sau
accelerate>=0.27.0 # v1.4: speedup cho diffusers
safetensors>=0.4.0 # v1.4: load weight cho diffusers
open3d>=0.18.0 # v1.5: Poisson surface reconstruction cho multi-view
